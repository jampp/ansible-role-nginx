---

nginx_user:  www-data
nginx_group: www-data

nginx_repo_packages:
  - nginx
  - nginx-full
  - nginx-common
  - openssl

nginx_compilation_packages: []

nginx_pid_file: /run/nginx.pid

# vi:ts=2:sw=2:et:ft=yaml
